普通模式：
	dd 	删除当前行
	ndd	删除n行
	yy	复制当前行
	p	光标处粘贴
	
	a	光标后一个字符处输入
	A	本行末尾处输入
	i	光标处输入
	o	新创建一行，行首处输入
		可以修改文本了
		
	j	光标移到下一行
	k	光标移到上一行
	x	删除光标后一个字符

最后一行的命令：
	shift + q 进入菜单设置模式：
		q 退出
		w 保存
		wq 保存并退出
