# 网站安全

解决方法之一：所以的目录下面都生成一个 index.php/index.html文件

	访问者因此不能看见该目录下面的文件信息(否则，直接从浏览器地址栏输入该路径，就会看见所有文件)
	
解决方法二：用户访问页面，都使用php文件访问，不使用html文件直接访问

	MCV
	
解决方法三：定义一个常量，正规访问才能进入，否则该常量未定义，直接跳转到指定页面

	比如：自己写一个404页面


